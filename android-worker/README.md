# Android Worker

The Android worker node that connects to the AndroCompute coordinator and executes distributed computing jobs.

## Features
- Automatic registration with coordinator
- Resource monitoring (CPU, memory, battery)
- Secure job execution
- Result submission

## Usage
```bash
pip install -r requirements.txt
python worker.py
